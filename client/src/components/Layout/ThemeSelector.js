export const ThemeSelector = () => {
    return (
        <div className="theme-selector">
            <a href={void (0)} className="spinner">
                <i className="ti-paint-bucket"></i>
            </a>
            <div className="body">
                <a href={void (0)} className="light"></a>
                <a href={void (0)} className="dark"></a>
            </div>
        </div>

    );
};